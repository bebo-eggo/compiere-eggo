package com.audaxis.compiere.eurocenter.interfaces;

/**
 * Interface for FieldName Transformer
 * The implementation of this interface should create a policy to transform fieldName
 * 
 * Ex Modify a specific fieldName:
 * 	public String transform(String fieldName) 
 *  {
 * 		String retValue = fieldName;
 * 		if ("myOldFieldName".equalsIgnoreCase(fieldName) 
 * 			retValue = "myNewFieldName";
 * 		return retValue;
 * 	}
 * 
 * 
 * @author steven, vca
 *
 */
public interface TransformerFieldName {
	
	public String transform(String fieldName);

}
