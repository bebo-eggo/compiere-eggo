package com.ecenter.compiere.util;

import org.compiere.model.MClient;
import org.compiere.util.Ctx;

import com.audaxis.compiere.model.proxy.MClientProxy;


public class EggoCtx {
		
	public static void setConstants(Ctx ctx, int AD_Client_ID) {
		
		MClient client = MClient.get(ctx, AD_Client_ID);
		MClientProxy clientProxy = new MClientProxy(client);
		
		//MERGE
	}
	
}
